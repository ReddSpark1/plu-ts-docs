---
sidebar_position: 14
---

# `PCurrencySymbol`

a bytestring of length 28 representing the hash of a minting policy (validator or native script).

definition:

```ts
export const PCurrencySymbol = palias( bs );
```

aliased type: [`TermBS`](../../stdlib/TermBS)