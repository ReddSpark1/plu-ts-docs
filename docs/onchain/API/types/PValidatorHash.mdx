---
sidebar_position: 10
---

# `PValidatorHash`

a bytestring of length 28 representing the hash of a validator.

definition:

```ts
export const PValidatorHash = palias( bs );
```

aliased type: [`TermBS`](../../stdlib/TermBS)