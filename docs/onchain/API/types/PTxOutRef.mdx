---
sidebar_position: 5
---

# `PTxOutRef`

It represents a transaction output.

definition:

```ts
export const PTxOutRef = pstruct({
    PTxOutRef: {
        id: PTxId.type,
        index: int
    }
});
```

### `id`

type: [`PTxId.type`](./PTxId)


### `index`

type: [`int`](../../stdlib/TermInt)
