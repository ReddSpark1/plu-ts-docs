---
sidebar_position: 12
---


# `PDatumHash`

a bytestring of length 28 representing the hash of a piece of `data` used as datum and included in the transaciton witnesses.

definition:

```ts
export const PDatumHash = palias( bs );
```

aliased type: [`TermBS`](../../stdlib/TermBS)