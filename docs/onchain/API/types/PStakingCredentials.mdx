---
sidebar_position: 8
---

# `PStakingCredentialss`

staking credentials; used as the second (optional) part of an address and responsabile of moving stake around.

definition:

```ts
export const PStakingCredentials = pstruct({
    PStakingHash: { _0: PCredential.type },
    PStakingPtr: {
        _0: int,
        _1: int,
        _2: int
    }
});
```

## `PStakingHash`

actual stake credentials (either pubKeyHash or validator hash)

### `_0`

type: [`PCredential`](./PCredential)

## `PStakingPtr`

stake credential pointer;

### `_0`

type: [`TermInt`](../../stdlib/TermInt)

### `_1`

type: [`TermInt`](../../stdlib/TermInt)

### `_2`

type: [`TermInt`](../../stdlib/TermInt)

